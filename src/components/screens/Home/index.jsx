import React from "react";

// Future home of some kind of user dashboard ?
// A Successful Login redirects user to the /home route
// This page could be used to aggregate recent posts within groups that user has joined. And notifications, messages, etc. Some kind of personalized stream could be interesting.

const Home = () => <div />;

export default Home;
