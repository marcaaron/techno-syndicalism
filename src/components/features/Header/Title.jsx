import React from "react";
import styled from "styled-components";

const Title = () => (
  <StyledTitle className="App-title">Welcome to React</StyledTitle>
);

const StyledTitle = styled.h1`
  font-size: 1.5em;
`;

export default Title;
